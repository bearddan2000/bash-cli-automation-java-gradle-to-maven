# bash-cli-automation-java-gradle-to-maven

## Description
Conversion of java gradle build
to maven build. 

## Tech stack
- bash

## To run
`sudo ./install.sh -u`

## To stop
`sudo ./install.sh -d`

## For help
`sudo ./install.sh -h`
